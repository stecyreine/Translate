{
  "name": "live-interpretation-tauri",
  "version": "1.0.0",
  "description": "Application de diffusion audio en direct (Tauri + Node.js)",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "dev:frontend": "vite",
    "build": "tauri build",
    "build:frontend": "vite build",
    "build:windows": "tauri build --target x86_64-pc-windows-msvc",
    "build:server": "node prepare-server.js",
    "preview": "vite preview",
    "type-check": "tsc --noEmit"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "@tauri-apps/api": "^1.5.0",
    "vite": "^5.0.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "socket.io-client": "^4.8.1"
  }
}
